# راهنمای نصب و راه‌اندازی - PDF Chatbot

## 📋 پیش‌نیازها

قبل از شروع، مطمئن شوید که موارد زیر را دارید:

1. ✅ **Python 3.8 یا بالاتر** نصب شده باشد
2. ✅ **OpenAI API Key** (از [platform.openai.com](https://platform.openai.com) دریافت کنید)
3. ✅ **فایل PDF** مستندات خود (در این مثال: shopmanual.pdf)
4. ✅ اتصال به اینترنت (برای دانلود کتابخانه‌ها و استفاده از API)

---

## 🚀 روش نصب و راه‌اندازی

### گام ۱: نصب Python

اگر Python ندارید:

**Windows:**
1. از [python.org](https://www.python.org/downloads/) دانلود کنید
2. حتماً گزینه "Add Python to PATH" را فعال کنید
3. Python را نصب کنید

**بررسی نصب:**
```bash
python --version
```

باید نسخه Python را نمایش دهد (مثلاً Python 3.11.0)

---

### گام ۲: آماده‌سازی پروژه

1. **فایل‌های پروژه را دانلود کنید** (یا از Git کلون کنید)

2. **ترمینال/CMD را در پوشه پروژه باز کنید**

3. **کتابخانه‌های مورد نیاز را نصب کنید:**

```bash
pip install -r requirements.txt
```

⏱️ این مرحله ممکن است چند دقیقه طول بکشد.

---

### گام ۳: تنظیم OpenAI API Key

1. **فایل `.env.example` را به `.env` کپی کنید** (یا یک فایل `.env` جدید بسازید)

2. **فایل `.env` را باز کنید** و کلید API خود را جایگزین کنید:

```env
OPENAI_API_KEY=sk-proj-your-actual-api-key-here
```

⚠️ **مهم:** این کلید حساس است و نباید آن را با دیگران به اشتراک بگذارید!

---

### گام ۴: قرار دادن فایل PDF

فایل PDF مستندات خود را در پوشه اصلی پروژه قرار دهید.

**نام پیش‌فرض:** `shopmanual.pdf`

اگر فایل شما نام دیگری دارد، در فایل `.env` تغییر دهید:

```env
PDF_FILE_PATH=your-document-name.pdf
```

---

### گام ۵: تست سیستم (اختیاری اما توصیه می‌شود)

برای اطمینان از درستی تنظیمات:

```bash
python test_system.py
```

این اسکریپت تمام بخش‌های سیستم را چک می‌کند و مشکلات احتمالی را نشان می‌دهد.

---

### گام ۶: راه‌اندازی اولیه پایگاه داده

**این مرحله بسیار مهم است و فقط یکبار لازم است.**

```bash
python initialize_db.py
```

**این اسکریپت:**
- 📖 متن را از PDF استخراج می‌کند
- ✂️ آن را به بخش‌های کوچکتر تقسیم می‌کند
- 🧮 Embedding ها را می‌سازد (با OpenAI)
- 💾 در پایگاه داده Vector ذخیره می‌کند

⏱️ **زمان مورد نیاز:**
- PDF 100 صفحه‌ای: ~5-10 دقیقه
- PDF 1000 صفحه‌ای: ~30-60 دقیقه

💡 **نکته:** این مرحله فقط یکبار انجام می‌شود. بعد از آن، سیستم آماده استفاده است.

---

### گام ۷: اجرای سرور

```bash
python run.py
```

یا در Windows می‌توانید فایل `start.bat` را دابل‌کلیک کنید.

**خروجی موفق:**

```
Starting FastAPI server...
Server will be available at: http://0.0.0.0:8000
API Documentation: http://0.0.0.0:8000/docs
```

---

### گام ۸: استفاده از چت‌بات

1. **مرورگر خود را باز کنید**

2. **به این آدرس بروید:**
   ```
   http://localhost:8000
   ```

3. **اگر پایگاه داده را initialize کرده‌اید:**
   - مستقیماً می‌توانید سوال بپرسید
   
4. **اگر initialize نکرده‌اید:**
   - روی دکمه "راه‌اندازی سیستم" کلیک کنید
   - منتظر بمانید (چند دقیقه)
   - سپس شروع به پرسیدن سوال کنید

---

## 🎯 مثال استفاده

**سوال:**
```
دستورالعمل تعمیر موتور چیست؟
```

**پاسخ:**
```
بر اساس مستندات، دستورالعمل تعمیر موتور به شرح زیر است:
1. ابتدا موتور را خاموش کنید
2. ...
[منابع: صفحه 45، صفحه 67]
[اطمینان: بالا]
```

---

## ⚙️ تنظیمات پیشرفته

### برای PDF های بزرگتر

اگر PDF شما خیلی بزرگ است (بیش از 1000 صفحه)، در فایل `.env`:

```env
CHUNK_SIZE=800
CHUNK_OVERLAP=150
```

### برای دقت بیشتر

```env
TOP_K_RESULTS=10
EMBEDDING_MODEL=text-embedding-3-large
```

### برای هزینه کمتر

```env
CHAT_MODEL=gpt-3.5-turbo
EMBEDDING_MODEL=text-embedding-3-small
TOP_K_RESULTS=3
```

---

## 🐛 رفع مشکلات رایج

### خطا: "OpenAI API key not found"

**راه‌حل:**
- مطمئن شوید فایل `.env` وجود دارد
- مطمئن شوید کلید API به درستی کپی شده
- مطمئن شوید فاصله یا کاراکتر اضافی نباشد

### خطا: "PDF file not found"

**راه‌حل:**
- نام فایل PDF را چک کنید
- مطمئن شوید در همان پوشه با اسکریپت‌ها است
- یا مسیر را در `.env` تنظیم کنید

### خطا: "Rate limit exceeded"

**راه‌حل:**
- API شما محدودیت دارد
- صبر کنید یا usage limits خود را بررسی کنید
- از account OpenAI خود استفاده کنید (نه trial)

### پاسخ‌های نامرتبط

**راه‌حل:**
- `TOP_K_RESULTS` را افزایش دهید
- `CHUNK_SIZE` را تنظیم کنید
- مطمئن شوید PDF به درستی استخراج شده
- مجدداً initialize کنید با `force_reprocess=True`

### کندی سیستم

**راه‌حل:**
- `CHUNK_SIZE` کوچکتر کنید
- `TOP_K_RESULTS` کاهش دهید
- از `gpt-3.5-turbo` استفاده کنید

---

## 📊 مدیریت هزینه‌ها

### برآورد هزینه برای PDF 1000 صفحه‌ای:

**راه‌اندازی اولیه (یکبار):**
- Embeddings: ~$0.50 - $2.00

**هر سوال:**
- Embedding query: ~$0.0001
- GPT-4 response: ~$0.01 - $0.05

💡 **نکته:** با استفاده از `gpt-3.5-turbo` هزینه‌ها حدود 10 برابر کمتر می‌شوند.

---

## 🔐 نکات امنیتی

1. ❌ **هرگز** کلید API را در کد قرار ندهید
2. ✅ **همیشه** از فایل `.env` استفاده کنید
3. ❌ فایل `.env` را commit نکنید (در `.gitignore` است)
4. ✅ کلید API را محرمانه نگه دارید
5. ✅ اگر کلید لو رفت، فوراً آن را regenerate کنید

---

## 📞 دریافت کمک

اگر مشکلی پیش آمد:

1. **لاگ‌ها را بخوانید** - پیام‌های خطا معمولاً راهنما هستند
2. **`test_system.py` را اجرا کنید** - مشکلات را شناسایی می‌کند
3. **README.md را مطالعه کنید** - اطلاعات تکمیلی دارد
4. **مستندات API** را ببینید: `http://localhost:8000/docs`

---

## ✅ چک‌لیست نهایی

قبل از شروع، مطمئن شوید:

- [ ] Python 3.8+ نصب شده
- [ ] کتابخانه‌ها نصب شده (`pip install -r requirements.txt`)
- [ ] فایل `.env` ساخته شده و API key تنظیم شده
- [ ] فایل PDF در جای مناسب است
- [ ] `initialize_db.py` اجرا شده (یا از UI اجرا می‌شود)
- [ ] سرور در حال اجراست (`python run.py`)
- [ ] صفحه وب باز می‌شود (`http://localhost:8000`)

---

## 🎉 موفق باشید!

حالا آماده‌اید که با مستندات خود چت کنید! 

**سوالات پیشنهادی برای شروع:**
- "این مستند درباره چیست؟"
- "فهرست مطالب چیست؟"
- "موضوع [X] را توضیح بده"
- "چطور [Y] را انجام دهم؟"

---

**نکته آخر:** اولین بار که سیستم را اجرا می‌کنید، صبور باشید. راه‌اندازی اولیه زمان می‌برد اما بعد از آن، پاسخ‌ها سریع هستند! 🚀



