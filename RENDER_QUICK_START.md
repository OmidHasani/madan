# راهنمای سریع دپلوی روی Render

این یک راهنمای سریع و ساده برای دپلوی پروژه روی Render.com است.

## مراحل سریع (5 دقیقه)

### 1️⃣ آماده‌سازی پروژه

```bash
# اطمینان حاصل کنید که همه فایل‌ها commit شده‌اند
git add .
git commit -m "Ready for Render deployment"
git push
```

### 2️⃣ ایجاد سرویس در Render

1. وارد [render.com](https://render.com) شوید
2. روی **"New +"** کلیک کنید
3. **"Web Service"** را انتخاب کنید
4. ریپازیتوری خود را متصل کنید

### 3️⃣ تنظیمات سرویس

#### Build Command:
```bash
pip install -r requirements.txt
```

#### Start Command:
```bash
uvicorn api:app --host 0.0.0.0 --port $PORT
```

### 4️⃣ Environment Variables

در بخش **"Environment"** این متغیرها را اضافه کنید:

**ضروری:**
```
OPENAI_API_KEY=sk-your-openai-key-here
```

**اختیاری (می‌توانید مقادیر پیش‌فرض را استفاده کنید):**
```
TAVILY_API_KEY=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
EMBEDDING_MODEL=text-embedding-3-large
CHAT_MODEL=gpt-4o
PDF_FILE_PATH=shopmanual.pdf
```

### 5️⃣ آپلود فایل PDF

**گزینه 1: استفاده از Git (برای فایل‌های کوچک)**
- فایل PDF را در root پروژه قرار دهید
- Commit و push کنید

**گزینه 2: استفاده از Persistent Disk (پیشنهادی)**
1. در تنظیمات سرویس، **"Persistent Disk"** را فعال کنید
2. بعد از اولین deploy، از طریق SSH فایل را آپلود کنید

**گزینه 3: استفاده از URL خارجی**
- اگر PDF در جای دیگری است، URL آن را در `PDF_FILE_PATH` قرار دهید

### 6️⃣ راه‌اندازی پایگاه داده

**روش 1: استفاده از Build Command (پیشنهادی)**

Build Command را به این صورت تغییر دهید:
```bash
pip install -r requirements.txt && python initialize_db.py || echo "Database init skipped"
```

**روش 2: استفاده از Web Interface**

بعد از deploy، به URL سرویس بروید و دکمه "راه‌اندازی سیستم" را بزنید.

### 7️⃣ دپلوی

1. روی **"Create Web Service"** کلیک کنید
2. منتظر بمانید تا build و deploy کامل شود
3. URL سرویس شما آماده است! 🎉

## استفاده از render.yaml (گزینه پیشرفته)

اگر می‌خواهید همه چیز را خودکار کنید:

1. فایل `render.yaml` در پروژه وجود دارد
2. در Render، **"New +"** > **"Blueprint"** را انتخاب کنید
3. ریپازیتوری را متصل کنید
4. Render به صورت خودکار همه چیز را تنظیم می‌کند

## نکات مهم

### ✅ Free Plan محدودیت‌ها:
- بعد از 15 دقیقه عدم استفاده، سرویس sleep می‌شود
- اولین درخواست بعد از sleep ممکن است کند باشد
- برای production، Paid Plan توصیه می‌شود

### ✅ Persistent Disk:
- برای نگهداری پایگاه داده، Persistent Disk را فعال کنید
- بدون Persistent Disk، داده‌ها بعد از هر deploy پاک می‌شوند

### ✅ Environment Variables:
- کلیدهای API را هرگز در کد commit نکنید
- فقط از Environment Variables استفاده کنید

## عیب‌یابی سریع

### Build فیل می‌شود؟
- بررسی کنید `requirements.txt` کامل است
- لاگ‌های Build را بررسی کنید

### سرویس شروع نمی‌شود؟
- بررسی کنید Start Command صحیح است
- بررسی کنید Environment Variables تنظیم شده‌اند

### خطای 500؟
- بررسی کنید پایگاه داده راه‌اندازی شده است
- بررسی کنید فایل PDF موجود است
- لاگ‌های Runtime را بررسی کنید

## بعد از دپلوی

1. به URL سرویس بروید
2. اگر پایگاه داده راه‌اندازی نشده، دکمه "راه‌اندازی سیستم" را بزنید
3. منتظر بمانید تا راه‌اندازی کامل شود
4. شروع به استفاده کنید! 🚀

## لینک‌های مفید

- [Render Dashboard](https://dashboard.render.com)
- [Render Docs](https://render.com/docs)
- [Python on Render](https://render.com/docs/deploy-python)

---

**سوال دارید؟** لاگ‌ها را در بخش "Logs" Render بررسی کنید.
