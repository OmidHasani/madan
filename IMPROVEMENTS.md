# 🎯 بهبودهای اعمال شده به چت‌بات

این فایل شامل توضیحات کامل بهبودهایی است که به سیستم چت‌بات اضافه شده است.

---

## 📋 خلاصه تغییرات

✅ **سیستم تشخیص Intent (نوع سوال)**
✅ **حافظه مکالمه (Conversation Memory)**
✅ **پرامپت تعاملی و هوشمند**
✅ **مدیریت Session های مختلف**
✅ **UI بهبود یافته برای تجربه کاربری بهتر**

---

## 🔍 1. سیستم تشخیص Intent (Intent Detection)

چت‌بات حالا می‌تونه **نوع سوال** کاربر رو تشخیص بده و بر اساس اون رفتار مناسب داشته باشه.

### انواع Intent:

#### 🔴 `ERROR_CODE` - کد خطا
- **مثال**: "CA1626"، "E15"، "CA135"
- **رفتار**: مستقیماً شرح کامل کد خطا، علل احتمالی، و مراحل عیب‌یابی رو میده
- **خروجی**: HTML فرمت‌شده با جزئیات کامل

#### 🔧 `TROUBLESHOOTING` - رفع مشکل
- **مثال**: "دستگاه کار نمیکنه"، "چطور این مشکل رو حل کنم؟"
- **رفتار**: سوالات دقیق‌تر میپرسه تا مشکل رو بهتر بفهمه
- **تعامل**: "آیا کد خطایی روی دستگاه نمایش داده می‌شه؟"

#### ℹ️ `INFORMATION` - سوال اطلاعاتی
- **مثال**: "شیر بای‌پس چیست؟"، "منظور از فشار هیدرولیک چیه؟"
- **رفتار**: توضیح واضح و کامل میده، در صورت نیاز مثال میزنه

#### ❓ `UNCLEAR` - سوال نامشخص
- **مثال**: سوالات کوتاه یا مبهم مثل "این"، "چرا؟"
- **رفتار**: سوالات روشن‌کننده میپرسه تا منظور کاربر رو بفهمه

### کد نمونه:
```python
def detect_intent(self, question: str) -> Dict:
    # تشخیص کد خطا با regex
    error_code_pattern = r'\b(ca|e|err|error)[\s\-]?\d+\b'
    
    # بررسی کلمات کلیدی
    troubleshooting_keywords = ['چطور', 'مشکل', 'خطا', 'رفع']
    info_keywords = ['چیه', 'چیست', 'تعریف', 'توضیح']
    
    # برگرداندن نوع Intent
    return {"intent": IntentType.ERROR_CODE, "confidence": "high"}
```

---

## 💬 2. حافظه مکالمه (Conversation Memory)

چت‌بات حالا **تاریخچه مکالمات** رو به خاطر میاره و میتونه به پیام‌های قبلی اشاره کنه.

### ویژگی‌ها:

#### 🆔 Session Management
- هر مکالمه یک **Session ID** منحصر به فرد داره
- Session ها در حافظه سرور نگهداری میشن
- تاریخچه هر session شامل پیام‌های کاربر و پاسخ‌های چت‌بات

#### 📜 History Tracking
- حداکثر **20 پیام** (10 تبادل) نگهداری میشه
- پیام‌های قدیمی‌تر به صورت خودکار حذف میشن تا context طولانی نشه
- تاریخچه همراه با هر سوال به مدل زبانی ارسال میشه

#### 🔄 Context Awareness
چت‌بات میتونه به مکالمات قبلی اشاره کنه:
```
کاربر: کد خطای CA1626 چیه؟
چت‌بات: [توضیح کامل...]

کاربر: علتش چیه؟  ← چت‌بات میدونه "علتش" اشاره به CA1626 هست
چت‌بات: همونطور که قبلاً گفتم، علل احتمالی CA1626 شامل...
```

### API Endpoints جدید:

```python
# دریافت تاریخچه یک session
GET /api/session/{session_id}

# پاک کردن تاریخچه
DELETE /api/session/{session_id}

# لیست تمام session ها
GET /api/sessions
```

---

## 🤝 3. پرامپت تعاملی و هوشمند

چت‌بات حالا **تعامل فعالانه** داره و وقتی سوال مبهم یا ناقص باشه، سوالات دقیق‌تر میپرسه.

### نمونه تعاملات:

#### سوال کوتاه:
```
کاربر: "مشکل"
چت‌بات: سلام! 👋

متوجه نشدم دقیقاً به چه چیزی نیاز دارید. لطفاً یکی از موارد زیر را انتخاب کنید:

🔴 کد خطا دارید? → مثلاً: CA1626
🔧 مشکلی دارید که نمی‌دونید چطور حلش کنید؟
ℹ️ سوال اطلاعاتی دارید؟
```

#### سوال مبهم:
```
کاربر: "دستگاه کار نمیکنه"
چت‌بات: سلام! 👋

برای اینکه بتونم دقیق‌تر کمکتون کنم:

🔹 آیا کد خطایی روی دستگاه نمایش داده می‌شه؟
🔹 علائم و مشکل دقیق چیه؟
🔹 این مشکل در چه شرایطی رخ میده؟
```

### System Prompts خاص Intent:

چت‌بات برای هر Intent یک دستورالعمل خاص داره:

```python
# ERROR_CODE Intent
"این سوال درباره یک کد خطای خاص است:
- شرح کامل کد خطا را بده
- علل احتمالی را کامل بیاور
- از فرمت HTML استفاده کن"

# TROUBLESHOOTING Intent
"این سوال درباره رفع مشکل است:
- ابتدا سوال کن: آیا کد خطایی نمایش داده می‌شه؟
- اگر علائم ناقص است، سوالات تکمیلی بپرس"

# INFORMATION Intent
"این سوال اطلاعاتی است:
- توضیح واضح و کامل بده
- اگر لازم است، مثال بزن"
```

---

## 🎨 4. بهبودهای UI/UX در Frontend

### دکمه پاک کردن مکالمه
- دکمه جدید **"🗑️ پاک کردن مکالمه"** در نوار بالا
- confirmation dialog برای جلوگیری از پاک شدن تصادفی
- پاک کردن session از سرور + تاریخچه محلی

### نگهداری Session در مرورگر
- `sessionId` به صورت محلی نگهداری میشه
- با هر پیام، `sessionId` به سرور ارسال میشه
- اولین پیام: session جدید ایجاد میشه
- پیام‌های بعدی: از همون session استفاده میشه

### نمایش اطلاعات Intent
- Intent تشخیص داده شده در console log نمایش داده میشه
- برای debugging و بهبود تجربه developer

---

## 🛠️ تغییرات تکنیکال

### فایل‌های تغییر یافته:

#### 1. `rag_engine.py`
```python
# اضافه شده:
- class IntentType(Enum)
- detect_intent() method
- generate_clarification_questions() method
- conversation_history parameter در answer_question()
- intent_info parameter در _generate_answer()
- system prompts بهبود یافته با توجه به Intent
```

#### 2. `api.py`
```python
# اضافه شده:
- conversation_sessions global dict
- session_id در QuestionRequest
- intent و needs_clarification در AnswerResponse
- مدیریت session در /api/ask
- GET /api/session/{session_id}
- DELETE /api/session/{session_id}
- GET /api/sessions
```

#### 3. `static/index.html`
```python
# اضافه شده:
- sessionId و conversationHistory متغیرها
- session_id در درخواست /api/ask
- clearConversation() function
- دکمه پاک کردن مکالمه
- استایل‌های جدید برای دکمه‌ها
```

---

## 📊 مقایسه قبل و بعد

### قبل:
❌ چت‌بات فقط به سوال فعلی پاسخ میداد
❌ نمی‌تونست به مکالمات قبلی اشاره کنه
❌ برای سوالات مبهم، پاسخ نامرتبط میداد
❌ تشخیص نوع سوال وجود نداشت

### بعد:
✅ چت‌بات تاریخچه کامل مکالمه رو به خاطر میاره
✅ میتونه به پیام‌های قبلی اشاره کنه
✅ برای سوالات مبهم، سوالات روشن‌کننده میپرسه
✅ تشخیص هوشمند Intent با confidence level
✅ تعامل محترمانه و دوستانه
✅ مدیریت session های مختلف

---

## 🚀 نحوه استفاده

### 1. راه‌اندازی سیستم:
```bash
python run.py
```

### 2. مکالمه نمونه:

```
👤 کاربر: سلام
🤖 چت‌بات: سلام! 👋
متوجه نشدم دقیقاً به چه چیزی نیاز دارید...

👤 کاربر: کد CA1626
🤖 چت‌بات: [شرح کامل کد خطا با HTML فرمت]

👤 کاربر: علتش چیه؟
🤖 چت‌بات: همونطور که قبلاً توضیح دادم، علل احتمالی CA1626 شامل...
[به مکالمه قبلی اشاره میکنه]

👤 کاربر: چطور رفعش کنم؟
🤖 چت‌بات: برای رفع کد خطای CA1626 که در مورد اون صحبت کردیم، مراحل زیر رو انجام بدید...
```

### 3. پاک کردن تاریخچه:
- روی دکمه "🗑️ پاک کردن مکالمه" کلیک کنید
- در dialog تایید کنید
- مکالمه جدید شروع میشه

---

## 🔮 قابلیت‌های آینده (پیشنهادی)

1. **ذخیره دائمی Session ها** در دیتابیس
2. **تحلیل احساسات** کاربر (Sentiment Analysis)
3. **پیشنهاد سوالات مرتبط** بعد از هر پاسخ
4. **صدا و ویدیو** برای راهنمایی عیب‌یابی
5. **چند زبانه بودن** کامل (فارسی/انگلیسی/عربی)
6. **یکپارچگی با Telegram/WhatsApp**
7. **داشبورد آماری** برای مدیریت

---

## 📞 پشتیبانی

اگر سوال یا مشکلی داشتید، لطفاً به موارد زیر مراجعه کنید:

- 📖 مستندات API: `http://localhost:8000/docs`
- 🔍 Console Logs در مرورگر برای debugging
- 📝 Log های سرور برای خطاهای backend

---

**تاریخ آپدیت**: {{ datetime.now().strftime('%Y-%m-%d') }}
**نسخه**: 2.0 - با قابلیت حافظه و تعامل هوشمند
