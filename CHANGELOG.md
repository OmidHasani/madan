# تغییرات پروژه

## تغییرات جدید

### ✅ افزودن قابلیت جستجوی وب (Tavily)
- افزودن ماژول `tavily_search.py` برای جستجو و استخراج محتوا از وب
- ادغام جستجوی وب با RAG Engine
- افزودن چک‌باکس "جستجوی وب" در رابط کاربری
- وقتی جستجو فعال باشد، محتوای استخراج شده از منابع وب به همراه سوال کاربر به GPT ارسال می‌شود

### ✅ رفع مشکل قفل شدن ورودی
- حالا می‌توانید حین دریافت پاسخ از AI، در قسمت ورودی تایپ کنید
- فقط دکمه ارسال در حین پردازش غیرفعال می‌شود

### ✅ ریسپانسیو برای موبایل
- بهینه‌سازی رابط کاربری برای نمایش در موبایل
- بهبود فونت‌ها و اندازه‌ها در صفحات کوچک
- بهبود چیدمان دکمه‌ها و فیلدهای ورودی
- بهینه‌سازی نمایش تصاویر در موبایل

### ✅ آماده‌سازی برای دپلوی
- افزودن فایل `.env.example` برای راهنمایی تنظیمات
- افزودن `DEPLOYMENT.md` با راهنمای کامل دپلوی
- افزودن `Procfile` برای Heroku
- افزودن `runtime.txt` برای Python version
- افزودن `wsgi.py` برای Gunicorn

### ✅ بهبودهای دیگر
- Auto-resize برای textarea
- بهبود مدیریت خطاها در جستجوی وب
- بهبود لاگ‌ها و پیام‌های خطا

## نحوه استفاده

### تنظیمات اولیه

1. کپی کردن `.env.example` به `.env`:
```bash
cp .env.example .env
```

2. ویرایش `.env` و وارد کردن کلیدهای API:
```env
OPENAI_API_KEY=your_openai_key
TAVILY_API_KEY=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
```

3. نصب وابستگی‌ها:
```bash
pip install -r requirements.txt
```

4. راه‌اندازی پایگاه داده:
```bash
python initialize_db.py
```

5. اجرای سرور:
```bash
python run.py
```

### استفاده از جستجوی وب

1. در رابط کاربری، چک‌باکس "🔍 جستجوی وب (Tavily)" را فعال کنید
2. سوال خود را بنویسید و ارسال کنید
3. سیستم به صورت خودکار:
   - در وب جستجو می‌کند
   - محتوای صفحات را استخراج می‌کند
   - محتوا را به همراه سوال شما به GPT می‌دهد
   - پاسخ بر اساس محتوای وب و مستندات تولید می‌شود

## فایل‌های جدید

- `tavily_search.py`: ماژول جستجو و استخراج محتوا
- `.env.example`: نمونه فایل تنظیمات
- `DEPLOYMENT.md`: راهنمای دپلوی
- `Procfile`: برای Heroku
- `runtime.txt`: نسخه Python
- `wsgi.py`: برای Gunicorn
- `CHANGELOG.md`: این فایل

## تغییرات در فایل‌های موجود

### `api.py`
- افزودن پارامتر `use_search` به `QuestionRequest`
- افزودن منطق جستجوی وب در endpoint `/api/ask`
- افزودن Tavily client به lifecycle

### `rag_engine.py`
- افزودن پارامتر `web_search_content` به متدهای `answer_question` و `_generate_answer`
- به‌روزرسانی system prompt برای استفاده از محتوای جستجو

### `config.py`
- افزودن `tavily_api_key` به Settings

### `static/index.html`
- افزودن چک‌باکس جستجو
- رفع مشکل قفل شدن ورودی
- بهبود ریسپانسیو برای موبایل
- افزودن auto-resize برای textarea

### `requirements.txt`
- افزودن `tavily-python`
- افزودن `beautifulsoup4`
- افزودن `requests`
- افزودن `python-dotenv`

## نکات مهم

1. **کلید Tavily**: اگر کلید Tavily تنظیم نشود، جستجوی وب غیرفعال می‌شود اما سیستم به کار خود ادامه می‌دهد
2. **هزینه**: استفاده از Tavily API ممکن است هزینه داشته باشد
3. **سرعت**: جستجوی وب ممکن است پاسخ را کندتر کند
4. **دپلوی**: برای دپلوی، فایل `DEPLOYMENT.md` را مطالعه کنید

## پشتیبانی

برای مشکلات یا سوالات:
1. بررسی لاگ‌های سرور
2. بررسی فایل `.env` و متغیرهای محیطی
3. بررسی مستندات در `DEPLOYMENT.md`
